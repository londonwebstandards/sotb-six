<!doctype html>
<html class="no-js" lang="en">
  <head>
    {{>head}}
    {{>favicons}}
    {{>twitter-card}}
    {{>analytics}}
  </head>
  <body>
    {{>nav}}

    {{>header}}

    <main class="site-content">

      {{#each collections.speakers}}
      <div class="row">
        <ul>
          <li>
            {{>speaker-card}}
          </li>
        </ul>
      </div>
      {{/each}}

      {{>page}}

    </main>

    {{>footer}}

    <script src="/assets/js/app.js"></script>
  </body>
</html>